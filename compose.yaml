services:
  web:
    image: oven/bun:1.1.29
    working_dir: /app
    command: sh -c "bun install && bun run dev -- -H 0.0.0.0 -p 3000"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true